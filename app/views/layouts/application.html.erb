<!DOCTYPE html>
<html>
  <head>
    <title><%= "#{content_for(:title)} | " if content_for?(:title) %>crosswords.lol</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= javascript_include_tag "application", defer: true %>
    <%= stylesheet_link_tag :application %>
  </head>
  <body>
    <div
      class="app"
      x-data="app"
      @popstate.window="loadPage(location)">
      <div
        class="loading-bar"
        x-data="loadingBar"
        :class="{loading: state === 'loading', loaded: state === 'complete'}">
        <div class="loading-bar-progress" :style="`width: ${progress}%`"></div>
      </div>
      <header class="app-header">
        <div class="app-header-container">
          <%= link_to root_path, class: "app-header-branding" do %>
            <%= image_tag "icon.svg", class: "app-header-logo", alt: "Logo for Crosswords.lol" %>
            <span class="app-header-name">crosswords.lol</span>
          <% end %>
          <div class="app-header-nav">
            <%= button_to devmode_path, method: :delete do %>
              <%= button :bug_off, tag: :span, alt: "Log out" if devmode? %>
            <% end %>
            <%= button :circle_user_round, href: params[:challenge] == "true" ? new_account_path(solution: true) : account_path, alt: "Account" if devmode? %>
            <%= button :settings, "@click": "toggleSettings", alt: "Settings" %>
          </div>
        </div>
      </header>
      <div data-morph-root>
        <%= content_for?(:main) ? yield(:main) : yield %>
      </div>
      <%= render "settings", back: ("Back to crossword" if @crossword) %>
    </div>
  </body>
</html>
